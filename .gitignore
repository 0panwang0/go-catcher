# ============================================================
# 编译产物（二进制走 GitHub Releases，不进仓库）
# ============================================================
*.exe
*.exe~

# Windows 资源对象：make build 由 winres/ 源文件自动生成
rsrc_windows_*.syso
*.syso

# ============================================================
# 用户运行时数据
# 包含 API 令牌、任务历史、本地磁盘路径、视频 URL —— 绝不上传。
# 故意不锚定根目录：程序按“当前工作目录”读写这些文件，
# 从任意子目录启动 exe 都会在原地生成同名文件。
# ============================================================
gocatcher_config.json
gocatcher_state.json
client_run.log

# 日志及轮转文件（gocatcher.log 超过 2MB 会轮转为 gocatcher.log.1）
gocatcher.log
gocatcher.log.*
*.log

# 原子写落盘时的中间文件（config.go: os.WriteFile(p+".tmp")）
*.tmp

# ============================================================
# 覆盖率 / 测试产物
# ============================================================
/cover
/coverage.out
/coverage.html
*.out
*.test
*.test.exe
*.test.log

# ============================================================
# 端到端 / 手工联调留下的临时目录
# ============================================================
/.tmp*/
/e2e_out/

# 根目录散落的下载测试残留：.ts 是 MPEG-TS 分片（非 TypeScript），
# 与 .m3u8 播放列表、.mp4 成品一样只是联调产物，不属于源码。
/*.ts
/*.m3u8
/*.mp4

# ============================================================
# 依赖
# ============================================================
node_modules/

# ============================================================
# 本地评审记录（整改报告等，属个人留档，不入仓库）
# ============================================================
docs/

# ============================================================
# IDE / 编辑器
# ============================================================
.idea/
.vscode/
.vs/
.trae/
.workbuddy/
*.swp
*.swo
*.code-workspace
*~

# ============================================================
# 系统
# ============================================================
.DS_Store
Thumbs.db
desktop.ini
